# Web #101 + #53 集成验收

基线：`main` 04ae9764；#101 7f5396b9；#53 a4666f8b。集成分支 `integrate/ship101-53`。

## 行为

- 保留 045 已发布计划后移、预览、服务端日期刷新及撤销。
- 合入 Cmd/Ctrl 多日选择、Shift 连续选择、跨周选择、按相对日偏移复制粘贴、批量清空及撤销。
- 遵守 SPEC-037：休息日由动作内容推导，不恢复 #53 旧版手动设休息日入口。
- 修复跨周导航后多选工具栏消失，以及粘贴动作后行选择与多日选择同时残留的问题；均先复现再补回归。

## 验证

- `npm test -- --run`：86 文件、810 项通过。
- `VITE_API_BASE='' npm run build`：TypeScript 与生产构建通过；仅既有大 chunk 提示。
- `git diff --cached --check`：通过。
- 独立 Standards / Spec 两轴审查：各一项相同 P2 已修复，复审均 CLEAN；多选 UI 14 项独立复跑通过。
- 浏览器本地隔离 fixture：多选工具栏显示“已选 2 天”；后移预览影响 2 天、跳过已完成 1 天；提交 +2 后推荐日期与后移标记刷新，已完成日期保持。
- 浏览器剪贴板模拟受限，复制/粘贴行为以回归测试为证；线上验收与部署 SHA 后续记录于后端部署台账。

## 上线顺序

后端 #276 → 数据库备份与 0070 → 新镜像全量部署 → `COACH_PLAN_SHIFT_ENABLED=true` → web 集成合并 → 同源 web 静态产物换装与线上验收。不得提前合并 web。

## 已上线（2026-09-16）

- 严格按上述顺序完成；web #101/#53/#102 均为 MERGED，部署源码 main `9cfc762cd1b8780b318029146721b202358611ed`。
- backend #278 镜像 `27bc3f7c938983ba6aeae9c3607a0b0d70415fbd`，[部署 run 35074778452](https://github.com/tianpingdeng112233-cell/MeetPR-backend/actions/runs/35074778452) 成功。0070、gate、备份与镜像正典为 [后端上线记录](https://github.com/tianpingdeng112233-cell/MeetPR-backend/blob/staging/docs/deployment-045-web101-53-2026-09-16.md)。
- 同源入口 `index--qJ0Upj8.js` SHA-256 `49f354df01de8d7e744316790e15c4cac652604bd017acec75be6cf5cc52372b`；线上 JS/CSS 均与构建逐字节一致。
- Chrome 既有教练会话的第一屏正常；点击训练日「后移…」可见真实预览（影响 2 天、已完成 18 天不动），Cmd 多选显示「已选 2 天 / 清空 2 天」。只读验收，没有修改真实学员日期/内容或发送通知。
- 截图与哈希证据在本机 `/Users/david/Projects/scratch/ship101-53-2026-09-16/`。APNS_ENV 仍为 sandbox；TestFlight production 推送实际送达未验收，不能把网页上线等同于推送全链路通过。
